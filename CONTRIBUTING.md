# Contributing to Baloot Game

شكراً لاهتمامك بالمساهمة في لعبة بلوت! 🎉

## كيفية المساهمة

### 1. الإبلاغ عن الأخطاء (Bug Reports)
- استخدم قسم Issues في GitHub
- وصف واضح للمشكلة
- خطوات إعادة إنتاج المشكلة
- بيئة التشغيل (المتصفح، نظام التشغيل)

### 2. اقتراح مميزات جديدة (Feature Requests)
- وصف الميزة المقترحة
- الفائدة المتوقعة
- أمثلة على الاستخدام

### 3. إرسال تعديلات (Pull Requests)
1. Fork المستودع
2. أنشئ فرعاً جديداً (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. افتح Pull Request

## معايير الكود

### JavaScript Style Guide
- استخدم ESLint configuration الموجودة
- أسماء متغيرات واضحة بالإنجليزية
- تعليقات بالعربية للمنطق المعقد
- JSDoc للدوال الرئيسية

### مثال:
```javascript
/**
 * يحسب نقاط الصكة الحالية
 * @param {Array} cards - الأوراق على الطاولة
 * @param {string} gameType - نوع اللعبة (sun/hukum)
 * @returns {number} المجموع
 */
function calculateTrickPoints(cards, gameType) {
    // منطق الحساب هنا
}
```

## مجالات تحتاج مساهمات

### 🎯 أولوية عالية
- [ ] تحسين ذكاء البوتات (AI)
- [ ] دعم اللغة الإنجليزية
- [ ] اختبارات وحدة (Unit Tests)
- [ ] تحسين الأداء

### 🎨 متوسط الأولوية
- [ ] تصميم أوراق مخصصة
- [ ] مؤثرات صوتية
- [ ] موسيقى خلفية
- [ ] أنيميشن إضافية

### 🔮 مستقبلية
- [ ] تطبيق موبايل (React Native/Flutter)
- [ ] نظام تسجيل دخول (OAuth)
- [ ] لوحة صدارة (Leaderboard)
- [ ] نظام أصدقاء

## قواعد الالتزام (Commit Rules)

استخدام التصنيفات التالية:
- `feat:` ميزة جديدة
- `fix:` إصلاح خطأ
- `docs:` تعديلات توثيق
- `style:` تعديلات تنسيق
- `refactor:` إعادة هيكلة
- `test:` اختبارات
- `chore:` مهام صيانة

### أمثلة:
```
feat: إضافة نظام الدردشة الصوتية
fix: إصلاح حساب نقاط الحكم
refactor: تحسين أداء ذاكرة البوت
```

## شكراً! 🙏

كل مساهمة - مهما كانت صغيرة - تساعد في تحسين اللعبة للجميع!